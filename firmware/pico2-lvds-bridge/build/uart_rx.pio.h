// -------------------------------------------------- //
// This file is autogenerated by pioasm; do not edit! //
// -------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// ---------- //
// uart_rx_8x //
// ---------- //

#define uart_rx_8x_wrap_target 0
#define uart_rx_8x_wrap 7
#define uart_rx_8x_pio_version 1

static const uint16_t uart_rx_8x_program_instructions[] = {
            //     .wrap_target
    0x2020, //  0: wait   0 pin, 0                   
    0xea27, //  1: set    x, 7                   [10]
    0x4001, //  2: in     pins, 1                    
    0x0642, //  3: jmp    x--, 2                 [6] 
    0x00c7, //  4: jmp    pin, 7                     
    0x20a0, //  5: wait   1 pin, 0                   
    0x0000, //  6: jmp    0                          
    0x8020, //  7: push   block                      
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program uart_rx_8x_program = {
    .instructions = uart_rx_8x_program_instructions,
    .length = 8,
    .origin = -1,
    .pio_version = uart_rx_8x_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config uart_rx_8x_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + uart_rx_8x_wrap_target, offset + uart_rx_8x_wrap);
    return c;
}

static inline void uart_rx_8x_program_init(PIO pio, uint sm, uint offset,
                                            uint rx_pin, uint baud)
{
    pio_sm_set_consecutive_pindirs(pio, sm, rx_pin, 1, false);  // input
    pio_gpio_init(pio, rx_pin);
    gpio_pull_up(rx_pin);           // UART idle = high
    pio_sm_config c = uart_rx_8x_program_get_default_config(offset);
    sm_config_set_in_pins(&c, rx_pin);
    sm_config_set_jmp_pin(&c, rx_pin);
    sm_config_set_in_shift(&c, true, false, 8);  // shift right (LSB-first), no autopush
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_RX);  // double RX FIFO depth (8 words)
    // Clock divider: PIO clock = baud × 8
    float div = (float)clock_get_hz(clk_sys) / (baud * 8);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

// ---------- //
// uart_rx_4x //
// ---------- //

#define uart_rx_4x_wrap_target 0
#define uart_rx_4x_wrap 7
#define uart_rx_4x_pio_version 1

static const uint16_t uart_rx_4x_program_instructions[] = {
            //     .wrap_target
    0x2020, //  0: wait   0 pin, 0                   
    0xe427, //  1: set    x, 7                   [4] 
    0x4001, //  2: in     pins, 1                    
    0x0242, //  3: jmp    x--, 2                 [2] 
    0x00c7, //  4: jmp    pin, 7                     
    0x20a0, //  5: wait   1 pin, 0                   
    0x0000, //  6: jmp    0                          
    0x8020, //  7: push   block                      
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program uart_rx_4x_program = {
    .instructions = uart_rx_4x_program_instructions,
    .length = 8,
    .origin = -1,
    .pio_version = uart_rx_4x_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config uart_rx_4x_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + uart_rx_4x_wrap_target, offset + uart_rx_4x_wrap);
    return c;
}

static inline void uart_rx_4x_program_init(PIO pio, uint sm, uint offset,
                                            uint rx_pin, uint baud)
{
    pio_sm_set_consecutive_pindirs(pio, sm, rx_pin, 1, false);
    pio_gpio_init(pio, rx_pin);
    gpio_pull_up(rx_pin);
    pio_sm_config c = uart_rx_4x_program_get_default_config(offset);
    sm_config_set_in_pins(&c, rx_pin);
    sm_config_set_jmp_pin(&c, rx_pin);
    sm_config_set_in_shift(&c, true, false, 8);  // LSB-first, no autopush
    sm_config_set_fifo_join(&c, PIO_FIFO_JOIN_RX);
    // Clock divider: PIO clock = baud × 4
    float div = (float)clock_get_hz(clk_sys) / (baud * 4);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

